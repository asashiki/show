# 控制台是 SPA（react-router, basename=/console）。
# 仅当请求的路径在仓库里【不存在】对应静态文件时，才回退到控制台入口，
# 由前端路由接管深链接 / 刷新。真实文件（index.html、assets/*）会被
# Cloudflare Pages 优先直接服务，不进入此重写规则，因此不会形成循环。
/console/*  /console/index.html  200
