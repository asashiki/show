# 控制台是 SPA（react-router, basename=/console）。
# /console 下没有对应静态文件的路径（如 /console/、/console/skills 等深链接 /
# 刷新）回退到 SPA 外壳，由前端路由接管。
#
# 注意：外壳文件 console-app.html 必须放在 /console/ 路径【之外】。
# Cloudflare Pages 会清理 URL（剥掉 .html 和 /index）——若目标写成
# /console/index.html，会被清理成 /console/，重新命中下面的 /console/* 而死循环。
# 目标 /console-app.html 清理后为 /console-app，不匹配 /console/*，因此安全。
#
# /console/assets/* 是真实文件，Pages 会优先直接服务，不进入此重写。
/console/*  /console-app.html  200
